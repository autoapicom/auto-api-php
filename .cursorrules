# auto-api-php — Cursor Rules

- PHP 8.1+, declare(strict_types=1) in every file
- Single Client class with 6 public methods
- Every method takes `source` as the first parameter
- Returns associative arrays, NO DTOs/value objects
- Guzzle 7 for HTTP, PSR-4 autoloading (AutoApi\ -> src/)
- api_key in query string for GET, x-api-key header for POST
- ApiException for errors, AuthException for 401/403
- English comments only

## Never

- Add unnecessary abstractions
- Create DTO or value object classes
- Use static methods
- Add dependencies beyond Guzzle
